Configuration Schema
====================

This page shows the schema of the exodus-lambda config file,
usually named ``lambda_config.json``, in `JSON schema`_ format
(encoded as YAML).

The latest version of this schema is available in raw form at
https://release-engineering.github.io/exodus-lambda/lambda_config.yaml.

.. include:: raw/lambda_config.yaml
    :code: yaml

.. _JSON schema: https://json-schema.org/
